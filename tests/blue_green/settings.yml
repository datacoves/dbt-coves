# Test and dbt-coves command settings
table: "TEST_MODEL"
schema: "TESTS_BLUE_GREEN"
database: "DBT_COVES_TEST"

blue_green:
  production_database: DBT_COVES_TEST
  staging_suffix: "STG"
  drop_staging_db_if_exists: True
